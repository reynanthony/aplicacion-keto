---
// AppNav — OBSOLETO tras el rediseño Editorial Bento.
// La bottom-nav móvil (5 items con botón central "Escanear") y el drawer "Más"
// viven ahora en AppSidebar.astro, montado globalmente por AppLayout.
// Se mantiene como stub para no romper las páginas que aún lo importan;
// el estado activo se detecta por URL en AppSidebar.
interface Props {
  active?: 'home' | 'macros' | 'plan' | 'recetas' | 'more';
}
Astro.props;
---
