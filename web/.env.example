# Monitoreo de errores (opcional). Sin esta variable, el build funciona
# igual pero sin Sentry activo. Se configura en Vercel > Project > Settings
# > Environment Variables, no hace falta tocar codigo.
SENTRY_DSN=
